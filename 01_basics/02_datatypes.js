"use strict"; //treat all js code as newer version

// alert (3 + 3); we are using node js not browser , it works when js engine is binded with browser

let flag = true;
let age = 25;
let name = "Anirudh";

// number 
// bigint
// string => ""
// boolean => true/false
// null = stand alone value (type = object)
// undefined = kindda place holder for a value which is not defined yet (type = undefined)
// symbol = uniquness

//object

console.log(typeof null); //object
console.log(typeof undefined); //undefined